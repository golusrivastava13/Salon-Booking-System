import { createTheme } from "@mui/material";

const blueTheme=createTheme({
    palette:{
        mode: "light",
        primary:{
            main: "#00C2FF"
        },
        secondary:{
            main: "#EAF0F1"
        }
    }
})

export default blueTheme;